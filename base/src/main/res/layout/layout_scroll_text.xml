<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/sl_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:scrollbarTrackVertical="@null"
    android:scrollbarStyle="outsideOverlay"
    android:scrollbarSize="2dp"
    android:scrollbarThumbVertical="@drawable/scrollbar_thumb"
    android:fadeScrollbars="false"
    android:paddingHorizontal="6dp"
    android:layout_marginHorizontal="6dp"
    app:layout_constraintHeight_max="40dp"
    app:layout_constraintHeight_min="20dp">

    <com.chinshry.base.view.FixedLineHeightTextView
        android:id="@+id/tv_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:ellipsize="end"
        android:textSize="@dimen/default_scroll_text_view_text_size"
        tools:text="@string/test_string_long" />

</ScrollView>